# Incentive Platform — Claude Rules

## What this project is

A **pre-PRD proposal**, not yet a build or even a scoped spec. Captured here so it has
a git home instead of living only in chat/memory. This is a documents/analysis project
for now — no code, no PRD yet.

## Workspace memory

Before project-specific work, also read:
- `../../AGENTS.md`
- relevant files in `../../knowledge/context/`

## Priority order

1. Latest user instruction
2. `docs/context/project_truth.md` — the proposal as currently understood
3. `docs/context/open_questions.md` — everything not yet decided (most of it, at this stage)

## State of this project

Problem statement and a solution direction exist (see `project_truth.md`); nothing is
locked, no PRD exists, no business validation has happened yet. Treat everything here
as a starting point for a PRD conversation, not as settled truth — surface it for
"present → debate → agree" before drafting anything further.
